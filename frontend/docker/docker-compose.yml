version: '3.5'

services:
  pika_build:
    build: ../
    container_name: 'pika_build'
    volumes:
      - dist:/usr/src/app/dist

  pika_web:
    image: nginx
    ports:
      - 80:80
    container_name: 'pika_web'
    restart: unless-stopped
    volumes:
      - dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf

volumes:
  dist:
