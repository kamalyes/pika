; Sample supervisor config file.
;
; For more information on the config file, please see:
; http://supervisord.org/configuration.html
;
; Notes:
;  - Shell expansion ("~" or "$HOME") is not supported.  Environment
;    variables can be expanded using this syntax: "%(ENV_HOME)s".
;  - Quotes around values are not supported, except in the case of
;    the environment= options as shown below.
;  - Comments must have a leading space: "a=b ;comment" not "a=b;comment".
;  - Command will be truncated if it looks like a config file comment, e.g.
;    "command=bash -c 'foo ; bar'" will truncate to "command=bash -c 'foo ".
;
; Warning:
;  Paths throughout this example file use /tmp because it is available on most
;  systems.  You will likely need to change these to locations more appropriate
;  for your system.  Some systems periodically delete older files in /tmp.
;  Notably, if the socket file defined in the [unix_http_server] section below
;  is deleted, supervisorctl will be unable to connect to supervisord.

[unix_http_server]
file={{SUPERVISOR_UNIX_HTTP_FILE}}   ; the path to the socket file

; Security Warning:
;  The inet HTTP server is not enabled by default.  The inet HTTP server is
;  enabled by uncommenting the [inet_http_server] section below.  The inet
;  HTTP server is intended for use within a trusted environment only.  It
;  should only be bound to localhost or only accessible from within an
;  isolated, trusted network.  The inet HTTP server does not support any
;  form of encryption.  The inet HTTP server does not use authentication
;  by default (see the username= and password= options to add authentication).
;  Never expose the inet HTTP server to the public internet.

[inet_http_server]            ; inet (TCP) server disabled by default
port={{SUPERVISOR_BIND}}:{{SUPERVISOR_PORT}}             ; ip_address:port specifier, *:port for all iface
username={{SUPERVISOR_USERNAME}}  ; default is no username (open server)
password={{SUPERVISOR_PASSWORD}}  ; default is no password (open server)

[supervisord]
logfile={{SUPERVISOR_LOGFILE}} ; main log file; default $CWD/supervisord.log
logfile_maxbytes={{SUPERVISOR_LOGFILE_MAXBYTES}}       ; max main logfile bytes b4 rotation; default 50MB
logfile_backups={{SUPERVISOR_LOGFILE_BACKUPS}}         ; # of main logfile backups; 0 means none, default 10
loglevel=info                ; log level; default info; others: debug,warn,trace
pidfile={{SUPERVISOR_PIDFILE}} ; supervisord pidfile; default supervisord.pid
nodaemon={{SUPERVISOR_DAEMON}} ; start in foreground if true; default false
silent=false                   ; no logs to stdout if true; default false
minfds={{SUPERVISOR_MINFDS}}                  ; min. avail startup file descriptors; default 1024
minprocs={{SUPERVISOR_MINPROCS}}              ; min. avail process descriptors;default 200
;umask=022                   ; process file creation umask; default 022
;user=supervisord            ; setuid to this UNIX account at startup; recommended if root
;identifier=supervisor       ; supervisord identifier, default is 'supervisor'
;directory=/tmp              ; default is not to cd during start
;nocleanup=true              ; don't clean up tempfiles at start; default false
;childlogdir=/tmp            ; 'AUTO' child log dir, default $TEMP
;environment=KEY="value"     ; key value pairs to add to environment
;strip_ansi=false            ; strip ansi escape codes in logs; def. false

; The rpcinterface:supervisor section must remain in the config file for
; RPC (supervisorctl/web interface) to work.  Additional interfaces may be
; added by defining them in separate [rpcinterface:x] sections.

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

; The supervisorctl section configures how supervisorctl will connect to
; supervisord.  configure it match the settings in eit/venv/bin/her the unix_http_server
; or inet_http_server section.

[supervisorctl]
serverurl=unix://{{SUPERVISOR_UNIX_HTTP_FILE}} ; use a unix:// URL  for a unix socket

[program: pika]
command=gunicorn -c {{WORKSPACES_PATH}}/gunicorn.py {{PIKA_RUN_COMMAND}}
directory={{WORKSPACES_PATH}}
user=root
stopsignal=INT
stdout_logfile={{WORKSPACES_PATH}}/logs/gunicorn_pika_info.log ; 错误日志文件
stderr_logfile={{WORKSPACES_PATH}}/logs/gunicorn_pika_error.log ; 输出日志文件
autostart=true                             ; 是否自启动
autorestart=true                           ; 是否自动重启
process_name=pika                          ; 进程名称
startsecs={{SUPERVISOR_STARTSECS}}         ; 自动重启时间间隔(s)
redirect_stderr = {{SUPERVISOR_REDIRECT_STDERR}}  ; 把 stderr 重定向到 stdout，默认 false

[program: proxy]
command=python -u proxy.py
directory={{WORKSPACES_PATH}}
user=root
stopsignal=INT
stdout_logfile={{WORKSPACES_PATH}}/logs/gunicorn_proxy_info.log
stderr_logfile={{WORKSPACES_PATH}}/logs/gunicorn_proxy_error.log
process_name=root
autostart=true                             ; 是否自启动
autorestart=true                           ; 是否自动重启
process_name=proxy                         ; 进程名称
startsecs={{SUPERVISOR_STARTSECS}}         ; 自动重启时间间隔(s)
stopwaitsecs={{SUPERVISOR_STOPWAITSECS}}   ; 等待的最大时间。超过这个时间，supervisord会向该子进程发送一个强制kill的信号

[include]
files = /etc/supervisor/config.d/*.conf